Main(n)
{
o=odd(n);
e=even(m);
}

odd(n)
{
  if(true){
     return 23;
  }
  else {
     ls=even(n.l);
     rs=even(n.r);
     return rs+ls+1;
  }
  
}

even(n){
 if (n==nil)
   return 0;
 else {
   ls=odd(n.l);
   rs=odd(n.r);
   return ls+rs;
 }
}