main(n)
{
  fused(n);  // 0
  o = n.v;
  e = n.s;  // 1
}

fused(n)
{
  if(n==nil){
     return;  // 2
  }
  else {
     fused(n.left);  // 3
     fused(n.right);  // 4
     n.v = n.left.s + n.right.s + 1;  // 5
     n.s = n.left.v + n.right.v;
     return;  // 6
  } 
}