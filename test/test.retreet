main(n)
{
o=odd(n);
e=even(n);   
}

odd(n)
{
  if(true){
     return 0;
  }
  else {
     ls=even(n.left);
     rs=even(n.right);
     return rs+ls+1;
  } 
}

even(m){
 if (n==nil){
   return 0;
 }
 else {
   ls=odd(n.left);
   rs=odd(n.right);
   ret = ls+rs;
   return ret;
 }
}